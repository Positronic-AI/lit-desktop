name: Build macOS DMG

# Manual trigger — builds a branded, unsigned .dmg for testers.
# Requires repo secret LIT_LIB_TOKEN: a token with read access to
# Positronic-AI/positronic-lit (to build the frozen backend on the Mac runner).
on:
  workflow_dispatch:
    inputs:
      brand:
        description: "Brand to build"
        type: choice
        options: [jovai, litai]
        default: jovai
      lib_ref:
        description: "positronic-lit branch/ref"
        default: develop

jobs:
  build-dmg:
    runs-on: macos-latest # Apple Silicon → aarch64-apple-darwin
    steps:
      - name: Checkout lit-desktop
        uses: actions/checkout@v4

      - name: Checkout positronic-lit (the backend)
        uses: actions/checkout@v4
        with:
          repository: Positronic-AI/positronic-lit
          ref: ${{ inputs.lib_ref }}
          token: ${{ secrets.LIT_LIB_TOKEN }}
          path: positronic-lit

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Freeze the backend (PyInstaller)
        working-directory: positronic-lit
        run: |
          python -m pip install --upgrade pip
          pip install . pyinstaller
          PYTHONPATH=src pyinstaller --noconfirm lit-server.spec

      - name: Stage the frozen backend as the Tauri sidecar
        run: |
          TRIPLE=$(rustc -vV | sed -n 's/^host: //p')
          echo "host triple: $TRIPLE"
          mkdir -p src-tauri/binaries
          cp "positronic-lit/dist/lit-server" "src-tauri/binaries/lit-server-${TRIPLE}"
          chmod +x "src-tauri/binaries/lit-server-${TRIPLE}"

      - name: Build the branded .dmg
        run: |
          npm install
          ./scripts/build-brand.sh "${{ inputs.brand }}"

      - name: Upload .dmg
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.brand }}-macos-dmg
          path: src-tauri/target/release/bundle/dmg/*.dmg
          if-no-files-found: error
